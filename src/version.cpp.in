#include "version.h"

#define DEF_GRAFT_VERSION_MAJOR 2
#define DEF_GRAFT_VERSION_MINOR 0
#define DEF_GRAFT_VERSION_PATCH 0

#define GRAFT_STRINGIFY2(val) #val
#define GRAFT_STRINGIFY(val) GRAFT_STRINGIFY2(val)
#define DEF_GRAFT_VERSION_STR GRAFT_STRINGIFY(DEF_GRAFT_VERSION_MAJOR) "." GRAFT_STRINGIFY(DEF_GRAFT_VERSION_MINOR) "." GRAFT_STRINGIFY(DEF_GRAFT_VERSION_PATCH) "_alpha"

#define DEF_GRAFT_VERSION_TAG "@VERSIONTAG@"
#define DEF_GRAFT_RELEASE_NAME "Name Me"

#define DEF_GRAFT_VERSION_FULL DEF_GRAFT_VERSION_STR "-" DEF_GRAFT_VERSION_TAG

#include "version.h"

const std::array<uint16_t, 3> GRAFT_VERSION = {DEF_GRAFT_VERSION_MAJOR, DEF_GRAFT_VERSION_MINOR, DEF_GRAFT_VERSION_PATCH};

const char* const GRAFT_VERSION_TAG = DEF_GRAFT_VERSION_TAG;
const char* const GRAFT_VERSION_STR = DEF_GRAFT_VERSION_STR;
const char* const GRAFT_RELEASE_NAME = DEF_GRAFT_RELEASE_NAME;
const char* const GRAFT_VERSION_FULL = DEF_GRAFT_VERSION_FULL;
